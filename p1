#!/bin/bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
PWD="$( pwd )"

#Compile source if necessary
if [ ! -f $DIR/"P1.class" ]
then
  cd $DIR
  make
  cd $PWD
fi;

java -cp $DIR P1 $1 $2 $3
